<configuration>

    <!-- Console Appender -->
    <appender name="ConsoleMe"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- File Appender -->
    <appender name="FileMe"
              class="ch.qos.logback.core.FileAppender">

        <file>journalApp.log</file>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} %-5level %logger - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- Root Logger -->
    <root level="DEBUG">

        <appender-ref ref="ConsoleMe"/>
        <appender-ref ref="FileMe"/>

    </root>

</configuration>